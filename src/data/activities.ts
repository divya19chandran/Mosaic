// Deprecated: the mock/curated activity list has been removed. Milestone 1
// starts Explore blank and sources activities from the user's own additions
// (see ../hooks/useMyActivities.ts) instead of a pre-seeded array.
// Left as an empty stub rather than deleted, per this folder's file-delete
// policy. Nothing in the app imports this file anymore.
export {};
