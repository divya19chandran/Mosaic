// Deprecated: superseded by ./useMyActivities.ts. In the new manual-add
// model, the user's added items ARE the primary content (not a "saved"
// bookmark on top of curated content). Left as an empty stub rather than
// deleted, per this folder's file-delete policy. Nothing in the app imports
// this file anymore.
export {};
