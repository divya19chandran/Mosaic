/**
 * DEPRECATED — superseded by CalendarView.tsx.
 *
 * Explore and Calendar used to be separate top-nav tabs, each reading
 * "my activities" independently. They're now merged into one Explore page
 * with a List/Calendar toggle (see ExplorePage.tsx), so the calendar body
 * moved to CalendarView.tsx (a props-driven component instead of a
 * standalone page) and nothing imports this file anymore.
 *
 * Left in place rather than deleted: this sandbox can't unlink files that
 * predate the current session inside the mounted project folder. Safe to
 * delete by hand from your own machine whenever convenient — it isn't
 * referenced from anywhere.
 */
export {};
