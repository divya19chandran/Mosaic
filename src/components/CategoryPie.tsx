// Deprecated: superseded by ./CategoryGrid.tsx (monochrome redesign — no
// per-category color coding or icons). Left as an empty stub rather than
// deleted, per this folder's file-delete policy. Nothing in the app imports
// this file anymore.
export {};
